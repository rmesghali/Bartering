<p id="notice"><%= notice %></p>

<table class="table table-hover">
  <thead>
    <tr>
      <th>Information</th>
    </tr>
    <tr>
      <th>Product</th>
      <th>Description</th>
      <th>Category</th>
      <th>Neighborhood</th>
      <th>Interested In</th>
      <th>Expiration</th>
      <th colspan="3"></th>
    </tr>
  </thead>
​
  <tbody>
    <tr>
      <td><%= @barter.product %></td>
      <td><%= @barter.description %></td>
      <td><%= @barter.category %></td>
      <td><%= @barter.neighborhood %></td>
      <td><%= @barter.interested_in %></td>
      <td><%= @barter.expiration %></td>
    </tr>
  </tbody>
</table>

</br>

<table class="table table-hover">
  <thead>
    <tr>
      <th>Offers</th>
    </tr>
    <tr>
      <th></th>
      <th></th>
      <th>Id</th>
      <th>User</th>
      <th>Message</th>
      <th>Message Timestamp</th>
    </tr>
  </thead>
  <tbody>
    <% @barter.comments.each do |comment| %>
    <tr>
      <% next if comment.accept == false %>
      <td><%= button_to 'Accept', comment_accept_barter_comments_path(@barter.id, comment.id), method: :post %></td>
      <td><%= button_to 'Decline', comment_decline_barter_comments_path(@barter.id, comment.id), method: :post %></td>
        <!-- <td><input type="button" value="Accept" name="accept_button"/></td>
        <td><input type="button" value="Decline" name="decline_button"/></td> -->
      <td><%= comment.id %></td>
      <td><%= comment.user.email %></td>
      <td><%= comment.comment %></td>
      <td><%= comment.created_at.to_formatted_s(:long_ordinal)  %></td>
    </tr>
    <% end %>
  </tbody>
</table>

</br>

<table class="table table-hover">
  <thead>
    <tr>
      <th>Comments</th>
    </tr>
    <tr>
      <th>Id</th>
      <th>User</th>
      <th>Message</th>
      <th>Message Timestamp</th>
    </tr>
  </thead>
  <tbody>
    <% @barter.comments.each do |comment| %>
    <tr>
      <% next if comment.accept == false %>
        <!-- <td><input type="button" value="Accept" name="accept_button"/></td>
        <td><input type="button" value="Decline" name="decline_button"/></td> -->
      <td><%= comment.id %></td>
      <td><%= comment.user.username %></td>
      <td><%= comment.comment %></td>
      <td><%= comment.created_at.to_formatted_s(:long_ordinal)  %></td>
    </tr>
    <% end %>
  </tbody>
</table>

  <!--Links to the the 'New Barter form to create a new item to trade'
  -->
<div class="link">
  <%= link_to 'Back', barters_path %> |
  <%= link_to 'New Comment', new_barter_comment_path(@barter.id) %> |
  <%= link_to "Let's Make a Trade", new_barter_comment_path(@barter.id) %>
</div>
<br/>
<br/>
<br/>
