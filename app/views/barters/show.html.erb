<p id="notice"><%= notice %></p>

<table class="table table-hover">
  <thead>
    <tr>
      <th>Product</th>
      <th>Description</th>
      <th>Category</th>
      <th>Neighborhood</th>
      <th>Expiration</th>
      <th colspan="3"></th>
    </tr>
  </thead>
​
  <tbody>
    <tr>
      <td><%= @barter.product %></td>
      <td><%= @barter.description %></td>
      <td><%= @barter.category %></td>
      <td><%= @barter.neighborhood %></td>
      <td><%= @barter.expiration %></td>
    </tr>
  </tbody>
</table>

<%= link_to "Let's Talk", new_barter_comment_path(@barter.id) %>
</br>

<table class="table table-bordered">
  <thead>
    <tr>
      <th>User</th>
      <th>Message</th>
      <th>Time</th>
    </tr>
  </thead>
  <tbody>
    <% @barter.comments.each do |comment| %>
    <tr>
      <td><%= comment.user.email %></td>
      <td><%= comment.comment %></td>
      <td><%= comment.created_at.to_formatted_s(:long_ordinal)  %></td>
    </tr>
    <% end %>
  </tbody>
</table>

  <!--Links to the the 'New Barter form to create a new item to trade'
  -->
<div class="link">
  <%= link_to 'Back', barters_path %> |
  <%= link_to 'New Comment', new_barter_comment_path(@barter.id) %>
</div>
<br/>
<form>
  <input type="button" value="Accept" name="accept_button"/>
  <input type="button" value="Decline" name="accept_button"/>
</form>
